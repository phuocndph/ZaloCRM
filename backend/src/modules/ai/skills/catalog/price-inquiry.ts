import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'price_inquiry', name: 'Price Inquiry', goal: 'Tra cứu giá hiện hành có citation.', activation: { intents: ['price_inquiry'], conditions: ['Current approved price list exists'] }, promptKey: 'skill.price_inquiry', knowledgeScope: { sourceTypes: ['price_list', 'product', 'policy'] }, allowedTools: ['context.read', 'knowledge.search'], allowedActions: ['suggest_reply', 'suggest_price'], approvalActions: ['send_price_commitment'], defaultTone: 'clear', safetyRules: ['Use only effective price sources', 'Never promise discounts'], handoffRules: ['Escalate if no current price citation'], confidenceThreshold: .78, riskTier: 'medium' });
