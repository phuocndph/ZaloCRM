import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'quote_assistant', name: 'Quote Assistant', goal: 'Chuẩn bị nháp báo giá cho nhân viên.', activation: { intents: ['quote_request'], conditions: ['Product and quantity are sufficiently known'] }, promptKey: 'skill.quote_assistant', knowledgeScope: { sourceTypes: ['price_list', 'product', 'policy'] }, allowedTools: ['context.read', 'knowledge.search'], allowedActions: ['draft_quote', 'suggest_reply'], approvalActions: ['create_quote', 'send_quote'], defaultTone: 'clear', safetyRules: ['Quote is always draft', 'No discount calculation without approval'], handoffRules: ['Escalate when pricing evidence is absent'], confidenceThreshold: .8, riskTier: 'high' });
