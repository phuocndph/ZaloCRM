import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'general_assistant', name: 'General Assistant', goal: 'Hỗ trợ nhân viên hiểu yêu cầu và đề xuất bước tiếp theo.', activation: { intents: ['greeting', 'unknown'], conditions: ['No specialist skill exceeds threshold'] }, promptKey: 'skill.general_assistant', knowledgeScope: { sourceTypes: ['faq', 'policy', 'manual'] }, allowedTools: ['context.read', 'knowledge.search'], allowedActions: ['suggest_reply'], approvalActions: [], defaultTone: 'warm', safetyRules: ['Never claim unknown facts', 'Do not send messages'], handoffRules: ['Escalate when user asks for human or confidence is low'], confidenceThreshold: .6, riskTier: 'low' });
