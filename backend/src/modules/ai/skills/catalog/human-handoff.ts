import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'human_handoff', name: 'Human Handoff', goal: 'Chuyển hội thoại cho nhân viên với tóm tắt an toàn.', activation: { intents: ['human_request', 'complaint', 'return_or_refund'], conditions: ['Human requested, escalation or low confidence'] }, promptKey: 'skill.human_handoff', knowledgeScope: { sourceTypes: [] }, allowedTools: ['context.read', 'summary.read'], allowedActions: ['create_handoff', 'summarize_case'], approvalActions: ['assign_owner'], defaultTone: 'handoff', safetyRules: ['Never pretend to be human', 'Do not send message automatically'], handoffRules: ['Always hand off request for human, safety, legal, anger or policy conflict'], confidenceThreshold: .5, riskTier: 'high' });
