import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'payment_reminder', name: 'Payment Reminder', goal: 'Soạn nháp nhắc thanh toán lịch sự.', activation: { intents: ['payment_inquiry'], conditions: ['Verified invoice or payment context'] }, promptKey: 'skill.payment_reminder', knowledgeScope: { sourceTypes: ['policy', 'faq'] }, allowedTools: ['context.read', 'invoice.read'], allowedActions: ['draft_payment_reminder'], approvalActions: ['send_payment_reminder'], defaultTone: 'concise', safetyRules: ['Do not expose payment details', 'Never claim overdue without verified invoice'], handoffRules: ['Escalate dispute or missing invoice'], confidenceThreshold: .82, riskTier: 'high' });
