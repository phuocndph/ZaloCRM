import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'complaint_handling', name: 'Complaint Handling', goal: 'Hỗ trợ tiếp nhận khiếu nại và chuyển nhân viên.', activation: { intents: ['complaint', 'return_or_refund'], conditions: ['Complaint or return signal'] }, promptKey: 'skill.complaint_handling', knowledgeScope: { sourceTypes: ['complaint_process', 'policy', 'warranty'] }, allowedTools: ['context.read', 'knowledge.search'], allowedActions: ['summarize_case', 'create_handoff'], approvalActions: ['send_reply', 'refund', 'return_authorization'], defaultTone: 'calm_deescalating', safetyRules: ['Acknowledge without admitting liability', 'Never promise refund'], handoffRules: ['Always hand off strong anger, refund, legal or safety issue'], confidenceThreshold: .72, riskTier: 'high' });
