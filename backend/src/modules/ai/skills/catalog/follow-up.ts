import { defineSkill } from '../skill-definition.js';
export default defineSkill({ key: 'follow_up', name: 'Follow-up', goal: 'Gợi ý follow-up phù hợp và tôn trọng khách.', activation: { intents: ['follow_up_response', 'hesitant'], conditions: ['Active follow-up or customer asks for time'] }, promptKey: 'skill.follow_up', knowledgeScope: { sourceTypes: ['consultation_script', 'faq'] }, allowedTools: ['context.read', 'followup.read'], allowedActions: ['suggest_reply', 'suggest_followup'], approvalActions: ['schedule_followup'], defaultTone: 'reassuring', safetyRules: ['Respect opt-out', 'No automatic sending'], handoffRules: ['Escalate stop-contact request'], confidenceThreshold: .65, riskTier: 'low' });
