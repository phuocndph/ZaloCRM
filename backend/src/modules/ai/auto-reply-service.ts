import { createHash } from 'node:crypto';
import { Prisma } from '@prisma/client';
import { prisma } from '../../shared/database/prisma-client.js';
import { buildConversationContext, ContextBuilderError, type ContextActor } from './conversation-context-builder-service.js';
import type { PolicyResult } from './policy-safety-checker-service.js';

export type AutoReplyMode = 'disabled' | 'shadow' | 'auto_send';
export type AutoReplyActor = ContextActor;
export type AutoReplyTransport = { send(input: { conversationId: string; text: string; idempotencyKey: string }): Promise<{ messageId?: string }> };
export class AutoReplyError extends Error { constructor(message: string, public readonly statusCode = 400, public readonly code = 'AUTO_REPLY_ERROR') { super(message); } }
const hash = (value: string) => createHash('sha256').update(value).digest('hex');
const riskyIntents = new Set(['complaint', 'return_or_refund', 'human_request', 'discount_request']);
function config(value: unknown) { return value && typeof value === 'object' && !Array.isArray(value) ? value as Record<string, any> : {}; }
function localHour() { return Number(new Intl.DateTimeFormat('en-GB', { timeZone: 'Asia/Ho_Chi_Minh', hour: '2-digit', hourCycle: 'h23' }).format(new Date())); }
function inHours(hours: unknown) { const entries = Array.isArray(hours) ? hours : []; if (!entries.length) return true; const hour = localHour(); return entries.some((row: any) => Number.isInteger(row?.start) && Number.isInteger(row?.end) && (row.start <= row.end ? hour >= row.start && hour < row.end : hour >= row.start || hour < row.end)); }
function mergeConfigs(rows: Array<any>) { return rows.reduce((all, row) => ({ ...all, ...config(row.config) }), {} as Record<string, any>); }
export async function updateAutoReplyConfig(actor: { orgId: string; userId: string }, input: { scope: 'workspace'|'zalo_account'|'employee'|'skill'|'intent'|'segment'; scopeRefId?: string | null; enabled?: boolean; mode?: AutoReplyMode; emergencyStop?: boolean; config?: Record<string, unknown> }) { if (!['workspace', 'zalo_account', 'employee', 'skill', 'intent', 'segment'].includes(input.scope)) throw new AutoReplyError('Invalid auto-reply scope', 400, 'INVALID_SCOPE'); if (input.scope !== 'workspace' && !input.scopeRefId) throw new AutoReplyError('scopeRefId is required for this scope', 400, 'SCOPE_REFERENCE_REQUIRED'); if (input.mode && !['disabled', 'shadow', 'auto_send'].includes(input.mode)) throw new AutoReplyError('Invalid auto-reply mode', 400, 'INVALID_MODE'); const current = await prisma.aiAutoReplyConfig.findFirst({ where: { orgId: actor.orgId, scope: input.scope, scopeRefId: input.scopeRefId ?? null, deletedAt: null }, orderBy: { updatedAt: 'desc' } }); const data = { enabled: input.enabled ?? current?.enabled ?? false, mode: input.mode ?? current?.mode ?? 'disabled', emergencyStop: input.emergencyStop ?? current?.emergencyStop ?? false, config: (input.config ?? current?.config ?? {}) as Prisma.InputJsonValue, createdByUserId: current?.createdByUserId ?? actor.userId }; const saved = current ? await prisma.aiAutoReplyConfig.update({ where: { id: current.id }, data }) : await prisma.aiAutoReplyConfig.create({ data: { orgId: actor.orgId, scope: input.scope, scopeRefId: input.scopeRefId ?? null, ...data } }); await prisma.aiAuditLog.create({ data: { orgId: actor.orgId, actorUserId: actor.userId, eventType: 'auto_reply.config_updated', outcome: 'success', targetType: 'ai_auto_reply_config', targetId: saved.id, metadata: { scope: saved.scope, enabled: saved.enabled, mode: saved.mode, emergencyStop: saved.emergencyStop } } }); return saved; }
export async function evaluateAutoReply(actor: AutoReplyActor, input: { conversationId: string; employeeId?: string; skillKey: string; intent: string; emotion: string; confidence: number; replyText: string; sources: Array<Record<string, any>>; policy: PolicyResult; requiresHuman?: boolean }) { const context = await buildConversationContext(actor, input.conversationId, { maxTokens: 1200 }); const conversation = await prisma.conversation.findFirst({ where: { id: input.conversationId, orgId: actor.orgId }, select: { id: true, zaloAccountId: true, contact: { select: { id: true, tags: true } } } }); if (!conversation) throw new AutoReplyError('Conversation not found', 404, 'CONVERSATION_NOT_FOUND'); const rows = await prisma.aiAutoReplyConfig.findMany({ where: { orgId: actor.orgId, deletedAt: null, OR: [{ scope: 'workspace', scopeRefId: null }, { scope: 'zalo_account', scopeRefId: conversation.zaloAccountId }, ...(input.employeeId ? [{ scope: 'employee', scopeRefId: input.employeeId }] : []), { scope: 'skill', scopeRefId: input.skillKey }, { scope: 'intent', scopeRefId: input.intent }] }, orderBy: { createdAt: 'asc' } }); const workspace = rows.find((row: any) => row.scope === 'workspace'); const settings = mergeConfigs(rows); const reasons: string[] = []; const enabledRows = rows.filter((row: any) => row.enabled && row.mode !== 'disabled'); const selected = enabledRows[enabledRows.length - 1] ?? workspace ?? null; const mode = (selected?.mode ?? 'disabled') as AutoReplyMode; if (rows.some((row: any) => row.scope === 'workspace' && row.emergencyStop)) reasons.push('EMERGENCY_STOP'); if (!selected?.enabled || mode === 'disabled') reasons.push('AUTO_REPLY_DISABLED'); if (!context.access.allowed || !context.access.contentVisible) reasons.push('CONVERSATION_ACCESS_DENIED'); const allowedIntents = Array.isArray(settings.allowedIntents) ? settings.allowedIntents : []; if (!allowedIntents.includes(input.intent)) reasons.push('INTENT_NOT_ALLOWED'); if (!input.sources?.length || input.sources.some((source) => !source?.citation && !source?.sourceType)) reasons.push('KNOWLEDGE_NOT_GROUNDED'); if (!input.policy.allowed || input.policy.requires_human) reasons.push('POLICY_BLOCKED'); const threshold = Number.isFinite(settings.confidenceThreshold) ? Number(settings.confidenceThreshold) : 85; if (input.confidence < threshold) reasons.push('CONFIDENCE_LOW'); if (riskyIntents.has(input.intent) || /discount|giảm giá/i.test(input.replyText)) reasons.push('RESTRICTED_INTENT_OR_DISCOUNT'); if (input.requiresHuman || input.emotion === 'angry' || input.emotion === 'disappointed') reasons.push('HUMAN_HANDOFF_REQUIRED'); if (settings.knowledgeConflict) reasons.push('KNOWLEDGE_CONFLICT'); if (!inHours(settings.businessHours)) reasons.push('OUTSIDE_BUSINESS_HOURS'); const segments = Array.isArray(settings.customerSegments) ? settings.customerSegments : []; const contactTags = Array.isArray(conversation.contact?.tags) ? conversation.contact.tags.filter((tag): tag is string => typeof tag === 'string') : []; if (segments.length && !segments.some((tag: string) => contactTags.includes(tag))) reasons.push('CUSTOMER_SEGMENT_NOT_ALLOWED'); const since = new Date(Date.now() - 24 * 60 * 60 * 1000); const maxMessages = Number.isFinite(settings.maxMessagesPerConversation) ? Number(settings.maxMessagesPerConversation) : 3; const sent = await prisma.aiAutoReplyLog.count({ where: { orgId: actor.orgId, conversationId: input.conversationId, status: { in: ['sent', 'auto_send_ready'] }, createdAt: { gte: since } } }); if (sent >= maxMessages) reasons.push('MESSAGE_LIMIT_REACHED'); const orgRecent = await prisma.aiAutoReplyLog.count({ where: { orgId: actor.orgId, status: { in: ['sent', 'auto_send_ready'] }, createdAt: { gte: new Date(Date.now() - 60 * 60 * 1000) } } }); const maxHourly = Number.isFinite(settings.maxMessagesPerHour) ? Number(settings.maxMessagesPerHour) : 30; if (orgRecent >= maxHourly) reasons.push('SPAM_RATE_LIMIT'); const allowed = reasons.length === 0; const status = !allowed ? 'blocked' : mode === 'shadow' ? 'shadow' : 'auto_send_ready'; const log = await prisma.aiAutoReplyLog.create({ data: { orgId: actor.orgId, conversationId: input.conversationId, configId: selected?.id ?? null, status, decisionReasons: reasons as Prisma.InputJsonValue, replyHash: hash(input.replyText), confidence: Math.max(0, Math.min(100, Math.round(input.confidence))), mode, metadata: { skillKey: input.skillKey, intent: input.intent, emotion: input.emotion, sourceCount: input.sources.length, threshold, shadow: mode === 'shadow', autoSendExecuted: false } } }); await prisma.aiAuditLog.create({ data: { orgId: actor.orgId, actorUserId: actor.userId, conversationId: input.conversationId, eventType: 'auto_reply.evaluated', outcome: status, targetType: 'ai_auto_reply_log', targetId: log.id, inputHash: hash(JSON.stringify({ skillKey: input.skillKey, intent: input.intent })), outputHash: log.replyHash, metadata: { reasons, mode, confidence: log.confidence, autoSendExecuted: false } } }); return { allowed, mode, status, reasons, logId: log.id, confidenceThreshold: threshold }; }
export async function processAutoReply(actor: AutoReplyActor, input: Parameters<typeof evaluateAutoReply>[1], transport?: AutoReplyTransport) { const decision = await evaluateAutoReply(actor, input); if (decision.status !== 'auto_send_ready') return decision; if (!transport) return { ...decision, status: 'auto_send_ready', sent: false, reason: 'No approved transport attached; message was not sent.' }; try { const sent = await transport.send({ conversationId: input.conversationId, text: input.replyText, idempotencyKey: decision.logId }); await prisma.aiAutoReplyLog.update({ where: { id: decision.logId }, data: { status: 'sent', metadata: { sentMessageId: sent.messageId ?? null, autoSendExecuted: true } } }); return { ...decision, status: 'sent', sent: true, messageId: sent.messageId ?? null }; } catch (error) { await prisma.aiAutoReplyLog.update({ where: { id: decision.logId }, data: { status: 'send_failed', metadata: { autoSendExecuted: true, error: error instanceof Error ? error.name : 'SEND_ERROR' } } }); throw new AutoReplyError('Auto reply transport failed', 502, 'AUTO_REPLY_SEND_FAILED'); } }
export { ContextBuilderError };